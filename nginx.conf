server {
    listen 8080;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    gzip on;

    location ~* \.(js|css|png|jpg)$ {
        expires 1y;
    }
}